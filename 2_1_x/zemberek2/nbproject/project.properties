application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
cekirdek.dir=${file.reference.src-cekirdek}
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
demo.dir=${file.reference.src-demo}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/zemberek2.jar
dist.javadoc.dir=${dist.dir}/javadoc
file.reference.junit-4.1.jar=lib/gelistirme/junit-4.1.jar
file.reference.src-az=src/az
file.reference.src-cekirdek=src/cekirdek
file.reference.src-demo=src/demo
file.reference.src-tm=src/tm
file.reference.src-tr=src/tr
file.reference.test-cekirdek=test/cekirdek
file.reference.test-tm=test/tm
file.reference.test-tr=test/tr
jar.compress=false
javac.classpath=
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${file.reference.junit-4.1.jar}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=net.zemberek.demo.DemoMain
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=${file.reference.src-az}
test.src.dir=${file.reference.test-cekirdek}
test.tm.dir=${file.reference.test-tm}
test.tr.dir=${file.reference.test-tr}
tm.dir=${file.reference.src-tm}
tr.dir=${file.reference.src-tr}
